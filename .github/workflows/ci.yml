name: CI - Pull Request

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  frontend-ci-pr:
    uses: empathyco/platform-reusable-github-actions/.github/workflows/frontend-ci-pr.yml@main
    secrets: inherit
  ci:
    needs: frontend-ci-pr
    runs-on: [self-hosted, small]
    steps:
      - run: echo "OK"
